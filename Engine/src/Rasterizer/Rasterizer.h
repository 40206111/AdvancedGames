#pragma once

#include "../Model.h"
#include "../Application.h"

class Rasterizer {
public:
	// constructor
	Rasterizer() {}

	// methods
	void draw(const Model &model, const Application &app);

private:

};